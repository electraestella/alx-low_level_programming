#include <stdio.h>
/**
 * main - The entry point of the program
 * Return: 0 (Upon successsful execution)
 */
int main(void)
{
int x;
for (x = 0; x <= 9; x++)
printf("%d", x);
putchar ('\n');
return (0);
}

